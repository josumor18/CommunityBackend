<%= form_for(community_member) do |f| %>
  <% if community_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(community_member.errors.count, "error") %> prohibited this community_member from being saved:</h2>

      <ul>
      <% community_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_community %>
    <%= f.number_field :id_community %>
  </div>

  <div class="field">
    <%= f.label :id_user %>
    <%= f.number_field :id_user %>
  </div>

  <div class="field">
    <%= f.label :isAdmin %>
    <%= f.check_box :isAdmin %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
